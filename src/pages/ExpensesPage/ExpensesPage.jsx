import { ExpenseForm, ExpensesView } from "../../layouts";

export default function ExpensesPage() {
    return (
        <main>
            <ExpenseForm />
            <ExpensesView />
        </main>
    );
}
