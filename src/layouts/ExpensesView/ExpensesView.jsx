import { ExpensesChart, ExpensesList } from "../../layouts";

export default function ExpensesView() {
    return (
        <section>
            <ExpensesChart />
            <ExpensesList />
        </section>
    );
}
